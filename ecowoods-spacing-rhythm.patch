diff --git a/apps/web/app/globals.css b/apps/web/app/globals.css
index ffb8cf9..465bcf2 100644
--- a/apps/web/app/globals.css
+++ b/apps/web/app/globals.css
@@ -109,10 +109,18 @@
   --font-body: 'Plus Jakarta Sans', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
   --font-mono: 'JetBrains Mono', 'Fira Code', ui-monospace, monospace;
 
-  /* Spacing rhythm */
+  /* Spacing rhythm
+     The middle term drives the fluid scale. It was 9vw, which is *viewport*-
+     proportional: sections drifted apart on wide monitors regardless of
+     content, reading as "floating". 5.5vw tracks content density more honestly
+     and the cap drops 7rem -> 5.5rem so large screens stay connected. Mobile is
+     effectively unchanged (min term 3.5rem vs 4rem). One scale, used everywhere,
+     is what makes rhythm feel intentional rather than incidental. */
   --shell-max: 1280px;
   --shell-pad: clamp(1.25rem, 4vw, 3rem);
-  --section-y: clamp(4rem, 9vw, 7rem);
+  --section-y: clamp(3.5rem, 5.5vw, 5.5rem);
+  --section-y-tight: clamp(2.25rem, 3.5vw, 3.5rem);
+  --section-head-gap: clamp(2rem, 3vw, 3rem);
   --header-h: 80px;
 
   /* Bottom-right/left floating UI. On phones a fixed CTA bar owns the last
@@ -293,8 +301,29 @@ p {
 }
 
 .section-tight {
-  padding-top: clamp(2.5rem, 5vw, 4rem);
-  padding-bottom: clamp(2.5rem, 5vw, 4rem);
+  padding-top: var(--section-y-tight);
+  padding-bottom: var(--section-y-tight);
+}
+
+/* Section headers previously had NO shared rule: the head->content gap was
+   whatever each following grid improvised (.standard-grid margin-top:3rem,
+   most others 0), so services floated above its cards while the gallery sat
+   flush. This owns that gap in one place. Inline maxWidth in page.tsx still
+   wins where set; this only supplies the vertical rhythm and a sensible
+   default width. */
+.section-head {
+  max-width: 720px;
+  margin-bottom: var(--section-head-gap);
+}
+.section-head .eyebrow {
+  margin-bottom: 0.9rem;
+}
+.section-head h2 {
+  margin: 0;
+}
+.section-head > p {
+  margin: 0.85rem 0 0;
+  max-width: 62ch;
 }
 
 .divider-leaf {
@@ -3386,7 +3415,6 @@ p {
   display: grid;
   grid-template-columns: repeat(4, 1fr);
   gap: 1.25rem;
-  margin-top: 3rem;
 }
 .standard-pillar {
   padding: 2rem 1.75rem;
