diff --git a/apps/web/app/globals.css b/apps/web/app/globals.css
index 89ffd91..1b535bd 100644
--- a/apps/web/app/globals.css
+++ b/apps/web/app/globals.css
@@ -4488,61 +4488,114 @@ html[data-theme='dark'] .gallery-tile:focus-within img {
 }
 
 .fc-plank {
+  /* THE ROOM.
+     Was a flat top-down swatch: a rectangle of stripes that told you the
+     colour and nothing about the floor. A floor is a surface you stand on and
+     look ACROSS, so the preview is now a perspective plane receding to a
+     horizon, with a wall behind it. Same four patterns, same tokens, no new
+     dependency and no canvas — CSS transforms only, so it costs nothing on
+     first paint and honours the global reduced-motion reset for free. */
   position: relative;
   aspect-ratio: 4 / 3;
   border-radius: var(--radius-md);
   overflow: hidden;
+  isolation: isolate;
+  perspective: 640px;
+  perspective-origin: 50% 34%;
+  /* the wall above the horizon, lit from the same side as the sheen */
+  background-image: linear-gradient(
+    180deg,
+    color-mix(in srgb, var(--fc-grain) 26%, #ffffff) 0%,
+    color-mix(in srgb, var(--fc-grain) 14%, #ffffff) 30%,
+    color-mix(in srgb, var(--fc-base) 45%, #ffffff) 38%,
+    transparent 39%
+  );
   background-color: var(--fc-base);
   box-shadow: inset 0 0 0 1px var(--line);
   transition: background-color 420ms var(--ease-out-quart);
 }
 
 .fc-plank::before {
-  /* Boards run horizontally, so grain lines run horizontally too. The old rule
-     put fine 3px stripes on the 90deg axis — across the boards — which read as
-     a barcode rather than as wood.
+  /* THE FLOOR PLANE.
+     Boards run AWAY from the viewer, so the seams are vertical in the
+     untransformed plane and converge once it is laid down — that convergence
+     is the whole reason this reads as three-dimensional.
        layer 1: fine grain, along the board
        layer 2: board-to-board seams
-       layer 3: staggered end joints  */
+       layer 3: staggered end joints, which compress toward the horizon  */
   content: '';
   position: absolute;
-  inset: -20%;
+  inset: 38% -75% -34%;
+  transform: rotateX(66deg);
+  transform-origin: 50% 0%;
+  transform-style: preserve-3d;
+  background-color: var(--fc-base);
   background-image:
-    repeating-linear-gradient(0deg, transparent 0 5px, color-mix(in srgb, var(--fc-grain) 20%, transparent) 5px 6px, transparent 6px 13px),
-    repeating-linear-gradient(0deg, color-mix(in srgb, var(--fc-grain) 55%, transparent) 0 2px, transparent 2px 54px),
-    repeating-linear-gradient(90deg, transparent 0 168px, color-mix(in srgb, var(--fc-grain) 38%, transparent) 168px 170px, transparent 170px 336px);
-  background-position: 0 0, 0 0, 0 0;
-  transition: background-image 420ms var(--ease-out-quart);
+    repeating-linear-gradient(90deg, transparent 0 7px, color-mix(in srgb, var(--fc-grain) 16%, transparent) 7px 8px, transparent 8px 19px),
+    repeating-linear-gradient(90deg, color-mix(in srgb, var(--fc-grain) 62%, transparent) 0 2px, transparent 2px 62px),
+    repeating-linear-gradient(0deg, transparent 0 118px, color-mix(in srgb, var(--fc-grain) 34%, transparent) 118px 120px, transparent 120px 236px);
+  transition: background-image 420ms var(--ease-out-quart), background-color 420ms var(--ease-out-quart);
 }
 
 .fc-plank::after {
-  /* finish tint */
+  /* Finish tint, plus the depth that perspective alone cannot give: the floor
+     darkens into the horizon and the corners fall away. Not transformed — it
+     sits over the whole frame. */
   content: '';
   position: absolute;
   inset: 0;
-  background: var(--fc-tint);
+  z-index: 1;
+  background-image:
+    linear-gradient(180deg, color-mix(in srgb, var(--fc-grain) 42%, transparent) 34%, transparent 62%),
+    radial-gradient(120% 80% at 50% 100%, transparent 45%, color-mix(in srgb, var(--fc-grain) 30%, transparent) 100%);
+  background-color: var(--fc-tint);
   transition: background 420ms var(--ease-out-quart);
 }
 
 .fc-plank-sheen {
+  /* Specular return off a floor, not a highlight across a swatch: a broad band
+     that runs with the boards and brightens toward the viewer. --fc-sheen is
+     set per finish, so matte barely lifts and satin reads wet. */
   position: absolute;
   inset: 0;
   z-index: 2;
-  background: linear-gradient(115deg, rgba(255, 255, 255, calc(var(--fc-sheen) * 2.4)) 0%, transparent 42%, transparent 70%, rgba(255, 255, 255, var(--fc-sheen)) 100%);
+  background-image:
+    linear-gradient(178deg, transparent 38%, rgba(255, 255, 255, calc(var(--fc-sheen) * 1.5)) 72%, rgba(255, 255, 255, calc(var(--fc-sheen) * 0.5)) 100%),
+    linear-gradient(102deg, transparent 30%, rgba(255, 255, 255, calc(var(--fc-sheen) * 1.1)) 55%, transparent 78%);
   transition: background 420ms var(--ease-out-quart);
 }
 
-.fc-plank--diagonal::before { transform: rotate(45deg) scale(1.5); }
+/* Pattern geometry is applied INSIDE the plane, before it is laid down, so
+   every pattern gets the same perspective for free. */
+.fc-plank--diagonal::before {
+  /* Rotate the GRADIENT ANGLES, never the element. `rotate(45deg)` on the
+     plane turned the plane's own edges too, so its corner cut a hard diagonal
+     across the wall above the horizon — the floor stopped being a floor. The
+     plane keeps the one shared rotateX; only the boards run at 45deg. */
+  background-image:
+    repeating-linear-gradient(45deg, transparent 0 7px, color-mix(in srgb, var(--fc-grain) 16%, transparent) 7px 8px, transparent 8px 19px),
+    repeating-linear-gradient(45deg, color-mix(in srgb, var(--fc-grain) 62%, transparent) 0 2px, transparent 2px 62px),
+    repeating-linear-gradient(135deg, transparent 0 118px, color-mix(in srgb, var(--fc-grain) 30%, transparent) 118px 120px, transparent 120px 236px);
+}
 .fc-plank--herringbone::before {
   background-image:
-    repeating-linear-gradient(45deg, color-mix(in srgb, var(--fc-grain) 70%, transparent) 0 1.5px, transparent 1.5px 34px),
-    repeating-linear-gradient(-45deg, color-mix(in srgb, var(--fc-grain) 70%, transparent) 0 1.5px, transparent 1.5px 34px),
-    repeating-linear-gradient(45deg, transparent 0 4px, color-mix(in srgb, var(--fc-grain) 20%, transparent) 4px 5px, transparent 5px 13px);
+    repeating-linear-gradient(45deg, color-mix(in srgb, var(--fc-grain) 68%, transparent) 0 2px, transparent 2px 42px),
+    repeating-linear-gradient(-45deg, color-mix(in srgb, var(--fc-grain) 68%, transparent) 0 2px, transparent 2px 42px),
+    repeating-linear-gradient(45deg, transparent 0 6px, color-mix(in srgb, var(--fc-grain) 18%, transparent) 6px 7px, transparent 7px 17px);
 }
 .fc-plank--chevron::before {
   background-image:
-    repeating-linear-gradient(60deg, color-mix(in srgb, var(--fc-grain) 70%, transparent) 0 1.5px, transparent 1.5px 30px),
-    repeating-linear-gradient(-60deg, color-mix(in srgb, var(--fc-grain) 70%, transparent) 0 1.5px, transparent 1.5px 30px);
+    repeating-linear-gradient(62deg, color-mix(in srgb, var(--fc-grain) 68%, transparent) 0 2px, transparent 2px 38px),
+    repeating-linear-gradient(-62deg, color-mix(in srgb, var(--fc-grain) 68%, transparent) 0 2px, transparent 2px 38px),
+    repeating-linear-gradient(0deg, transparent 0 76px, color-mix(in srgb, var(--fc-grain) 26%, transparent) 76px 78px, transparent 78px 152px);
+}
+
+/* A 3-D transform is the one thing that can make a still image feel unstable
+   to a vestibular-sensitive reader. Flatten it and keep the swatch honest. */
+@media (prefers-reduced-motion: reduce) {
+  .fc-plank { perspective: none; background-image: none; }
+  .fc-plank::before { inset: 0; transform: none; }
+  .fc-plank::after { background-image: none; }
 }
 
 .fc-spec {
diff --git a/audit/FINDINGS.md b/audit/FINDINGS.md
index 1a40d92..c336add 100644
--- a/audit/FINDINGS.md
+++ b/audit/FINDINGS.md
@@ -2319,3 +2319,41 @@ silently orphan one. Measured: one distinct width per group, at every width.
 
 **Verified:** four guards green, parse-scan 0, undefined tokens 0, no type
 errors in `FloorConfigurator.tsx` or `globals.css`.
+
+---
+
+### F-67 · The floor preview was a swatch, not a floor · P2
+
+`/design` sells a surface you stand on and look **across**. The preview was a
+flat top-down rectangle of stripes: it told you the colour and nothing about the
+floor. Rebuilt as a perspective plane receding to a horizon, with a wall behind
+it and the boards running away from the viewer — that convergence is the whole
+reason it reads as three-dimensional.
+
+CSS transforms only. No canvas, no WebGL, no new dependency, nothing to
+hydrate, and the global reduced-motion reset already covers the transitions.
+Three layers on the existing markup, so the component's `role="img"` and its
+per-configuration `aria-label` are untouched:
+
+- `.fc-plank` — the room: `perspective: 640px` plus a wall gradient above the
+  horizon.
+- `::before` — the floor plane, laid down with `rotateX(66deg)` from a top
+  origin. Board seams are vertical in the untransformed plane so they converge;
+  end joints are horizontal so they compress toward the horizon.
+- `::after` — finish tint, plus the depth perspective alone cannot give: the
+  floor darkens into the horizon and the corners fall away.
+- `.fc-plank-sheen` — a specular return that runs with the boards and brightens
+  toward the viewer, scaled by the per-finish `--fc-sheen`, so matte barely
+  lifts and satin reads wet.
+
+**One defect caught by rendering all 24 species × finish × pattern combinations
+rather than trusting the code:** `diagonal` was written as
+`transform: rotateX(66deg) rotate(45deg)`, which rotated the **plane**, not the
+pattern — the plane's own corner cut a hard diagonal across the wall above the
+horizon and the floor stopped being a floor. Fixed by rotating the gradient
+angles instead and leaving every pattern on the one shared `rotateX`. Herringbone
+and chevron already worked this way; diagonal was the odd one out.
+
+Reduced motion flattens it back to the honest top-down swatch: a still image
+with strong 3-D perspective is exactly the kind of thing that unsettles a
+vestibular-sensitive reader, and the colour information survives the flattening.
